# Prompt & UI Improvement Plan

## Goal Description
The user reported "Manga not generated" because they were copying the "Backup" data (which the AI interprets as context only) instead of the individual "Page Generation" prompts. I will clarify the UI and the prompt text to guide the user/AI correctly.

## User Review Required
None.

## Proposed Changes
### Prompt Logic
#### [MODIFY] [src/utils/manga/prompt/generator.js](file:///c:/Users/hirak/Desktop/%E6%BC%AB%E7%94%BB/src/utils/manga/prompt/generator.js)
- Update `fullOutput` header to explicitly state "DO NOT GENERATE IMAGES FROM THIS DATA ALONE" or "Ask the user specifically which page to generate".
- Add a "System Note" to the AI in the backup prompt: "If the user provided this, they might want to start generation. Suggest starting with Page 1."

### UI
#### [MODIFY] [src/components/output/YamlOutput.jsx](file:///c:/Users/hirak/Desktop/%E6%BC%AB%E7%94%BB/src/components/output/YamlOutput.jsx)
- Rename "Backup" button to "Project Data (Backup)".
- Change styling to make "Page 1" button the primary call-to-action (e.g., larger, different color).
- Update tooltip/help text.

## Verification Plan
### Manual Verification
- Verify the copied text changes.
- Verify the UI button hierarchy.
- (Self-Correction) Since I cannot paste into the user's AI, I will verify the produced text strings via `console.log` or reading the file.
