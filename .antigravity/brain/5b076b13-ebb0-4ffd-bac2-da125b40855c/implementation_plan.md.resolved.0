# Implementation Plan - Refine EP15.5

## Goal
Standardize `EP15.5_黄金の果樹園.md` to match the quality and format of recent episodes (specifically `EP16`). This involves fixing encoding issues and updating the image generation prompts to use the "Drama Boost" style.

## User Review Required
- **Encoding Change**: Converting `EP15.5` from Shift-JIS to UTF-8 to match `EP16`.
- **Prompt Style**: Adding `(Boost) Lock + Drama Boost` parameters to the prompts.

## Proposed Changes

### Story Folder
#### [MODIFY] [EP15.5_黄金の果樹園.md](file:///c:/Users/hirak/Desktop/2nd-Brain/18_レミ投資漫画/マンガノ/01_長編_希望の投資/01_ストーリー/EP15.5_黄金の果樹園.md)
- **Encoding**: Convert file to UTF-8.
- **Content**: Update `generate_image` blocks.
  - Page 75.5-A: Add `(Boost) Lock + Drama Boost` to Remi's visual lock.
  - Page 75.5-B: Add `(Boost) Lock + Drama Boost` to Remi's visual lock.
  - Ensure `STYLE` section matches `EP16` (likely identical "Edge Obliterator").

#### [MODIFY] [00_新規生成リスト_ここからスタート.md](file:///c:/Users/hirak/Desktop/2nd-Brain/18_レミ投資漫画/マンガノ/01_長編_希望の投資/01_ストーリー/00_新規生成リスト_ここからスタート.md)
- Mark `EP15.5` as Completed `[x]`.

## Verification Plan

### Manual Verification
- **Read Content**: Use `type` command (or Python script) to verify `EP15.5` is readable and contains the new prompt parameters.
- **Check Encoding**: Verify `EP15.5` is UTF-8 (should allow special characters without Mojibake in UTF-8 environments).
- **Check Checklist**: Verify `00_新規生成リスト` has `[x]` for EP15.5.
